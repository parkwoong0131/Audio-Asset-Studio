pyyaml>=6.0
pydantic>=2.0
pydub>=0.25.1
librosa>=0.10.0
numpy>=1.24
requests>=2.28

# --- local inference (M-series Mac MPS) ---
torch>=2.3
torchaudio>=2.3
audiocraft>=1.3.0           # MusicGen / AudioGen / MusicGen-Melody
# stable-audio-tools>=0.0.16  # optional — install manually if needed
soundfile>=0.12

# --- loudness / tagging / stems ---
pyloudnorm>=0.1.1
laion-clap>=1.1.6           # CLAP audio embeddings / tagging
demucs>=4.0.0               # stem separation

# --- prompt library (vector store) ---
chromadb>=0.4.22

# --- warm-pool server ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- dashboard ---
streamlit>=1.34
matplotlib>=3.8
openpyxl>=3.1
